<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/boardList"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/moveToTop"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="20dp"
        android:layout_marginRight="20dp"
        android:layout_marginBottom="10dp"
        android:backgroundTint="@android:color/white"
        android:contentDescription="@drawable/icon_top"
        android:visibility="gone"
        app:borderWidth="0dp"
        app:fabCustomSize="44dp"
        app:layout_constraintBottom_toTopOf="@id/boardAdd"
        app:layout_constraintEnd_toEndOf="parent"
        app:maxImageSize="16dp"
        app:srcCompat="@drawable/icon_top"
        tools:targetApi="lollipop"
        tools:visibility="visible"/>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/boardAdd"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="20dp"
        android:layout_marginRight="20dp"
        android:layout_marginBottom="30dp"
        android:backgroundTint="@android:color/white"
        android:contentDescription="@drawable/icon_add"
        app:borderWidth="0dp"
        app:fabCustomSize="44dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:maxImageSize="16dp"
        app:srcCompat="@drawable/icon_add"
        tools:targetApi="lollipop" />

</androidx.constraintlayout.widget.ConstraintLayout>